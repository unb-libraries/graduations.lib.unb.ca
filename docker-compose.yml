version: '3.6'

services:
  drupal-mysql-lib-unb-ca:
    image: mysql:5.7
    restart: always
    command: mysqld --skip-name-resolve --max_allowed_packet=64M
    env_file:
      - ./env/mysql.env
    expose:
      - "3306"
    networks:
      - graduations.lib.unb.ca

  drupal-redis-lib-unb-ca:
    image: redis:5-alpine
    restart: always
    expose:
      - "6379"
    networks:
      - graduations.lib.unb.ca
    env_file:
      - ./env/redis.env

  drupal-solr-lib-unb-ca:
    image: unblibraries/solr-drupal:8.x-4.x
    env_file:
      - ./env/solr.env
    expose:
      - "8983"
    networks:
      - graduations.lib.unb.ca
    entrypoint:
      - docker-entrypoint-multiple.sh
      - ceremonies.graduations.lib.unb.ca:/solr-conf

  graduations.lib.unb.ca:
    build:
      context: .
    restart: always
    container_name: graduations.lib.unb.ca
    expose:
      - "80"
    ports:
      - "3088:80"
    env_file:
      - ./env/drupal.env
    networks:
      - graduations.lib.unb.ca
    volumes:
      - /app/html/sites/default
      - ./config-yml:/app/configuration
      - ./custom/modules:/app/html/modules/custom
      - ./custom/themes:/app/html/themes/custom
      - ./tests:/app/tests

  mailhog:
    image: mailhog/mailhog
    expose:
      - "1025"
      - "8025"
    ports:
      - "4088:8025"
    networks:
      - graduations.lib.unb.ca

networks:
  graduations.lib.unb.ca:
    name: graduations.lib.unb.ca
