id: ceremonies
label: Ceremonies
migration_group: honorary_migration

source:
  plugin: ceremonies

destination:
  plugin: entity:node

process:
  type:
    plugin: default_value
    default_value: ceremony

dependencies:
  module:
    - honorary_migration
